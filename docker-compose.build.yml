# Local/CI overlay for docker-compose.yaml: deployed environments pull from GHCR.
services:
  va-springboard-app:
    build:
      context: .
      dockerfile: Dockerfile
  va-springboard-migrations:
    build:
      context: .
      dockerfile: Dockerfile
