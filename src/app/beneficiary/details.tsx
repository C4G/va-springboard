'use client';

import * as React from 'react';

type BeneficiaryDetailsProps = {
  id?: string;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function BeneficiaryDetails({ id }: BeneficiaryDetailsProps) {
  return (
    <div className='h-full w-full rounded-md border p-8'>Work in Progress</div>
  );
}
